# Nixpacks configuration for Railway - Node.js/Express Backend
# Backend files are at root level

[phases.setup]
nixPkgs = { node = "18" }

[phases.install]
cmds = [
  "npm install"
]

[phases.build]
cmds = [
  "echo 'Build complete'"
]

[start]
cmd = "node app.js"
